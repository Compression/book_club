<% if @books.empty? %>
  <p>No books yet!</p>
<% else %>
  <ul>
    <% @books.each do |book| %>
      <li>
        <strong><%= link_to book.title, book_path(book) %> | <%= book.author %></strong><br>
      </li>
    <% end %>
  </ul>
<% end %>

<%= link_to 'Submit a book!', new_book_path %>
